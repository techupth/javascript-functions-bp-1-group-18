// Exercise #4 Grouping Function with Objects

let calculator = {
    add: (a, b) => a + b,
    subtract: (a, b) => a - b,
    multiply: (a, b) => a * b,
    divide: (a, b) => a / b,
};


let resultAdd = calculator.add(10, 20);
console.log(resultAdd);


